const mongoose = require("mongoose");
const Course = require("../models/Course");

function editCourse() {
    // finish this function
    // the arguments of this function will be the courseId and the updated fields
}